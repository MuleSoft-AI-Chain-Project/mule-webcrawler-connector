<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.mulesoft.connectors</groupId>
    <artifactId>mule4-webcrawler-connector</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>mule-extension</packaging>
    <name>MuleSoft WebCrawler Connector - Mule 4</name>
	<description>The MuleSoft WebCrawler Connector enables a Mule application to crawl websites and retrieve content, potentially for creating vector embeddings for structured knowledge extraction.</description>
	<url>https://mac-project.ai/docs/ms-webcrawler/connector-overview</url>
	<scm>
		<connection>scm:git:git://github.com/MuleSoft-AI-Chain-Project/mule-web-crawler-connector.git</connection>
		<developerConnection>scm:git:ssh://github.com:MuleSoft-AI-Chain-Project/mule-web-crawler-connector.git</developerConnection>
		<url>https://github.com/MuleSoft-AI-Chain-Project/mule-web-crawler-connector/tree/master</url>
	</scm>

	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>

    <parent>
        <groupId>com.mulesoft.connectors</groupId>
        <artifactId>mule4-connectors-parent</artifactId>
        <version>5.0.4</version>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jdk.version>11</jdk.version>
        <runtimeProduct>MULE_EE</runtimeProduct>
        <runtimeVersion>4.6.27</runtimeVersion>
        <muleJavaEeBomVersion>4.6.0</muleJavaEeBomVersion>
        <mule.sdk.version>1.5.1</mule.sdk.version>
        <mule.api.version>1.5.1</mule.api.version>
        <jsoup.version>1.18.3</jsoup.version>
        <selenium.version>4.35.0</selenium.version>
        <muleObjectStoreConnectorVersion>1.2.5</muleObjectStoreConnectorVersion>
        <munit.failIfNoTests>false</munit.failIfNoTests>
        <skipVerifications>true</skipVerifications>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.mule</groupId>
                <artifactId>mule-javaee-runtime-bom</artifactId>
                <version>${muleJavaEeBomVersion}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Selenium 4.35 transitively pulls byte-buddy 1.17.6, whose stricter method
                 resolution breaks the bsc maven-processor-plugin's runtime model build (the
                 parser blows up with "Cannot resolve ambiguous delegation" on Object.clone).
                 Pin to 1.14.0 — the version master resolves to via mule-modules-parent 1.5.1 —
                 to keep the processor working. Same version as master, no downgrade vs. master. -->
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>1.14.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>${jsoup.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.2</version>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20240303</version>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>2.16.1</version>
        </dependency>

        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-api</artifactId>
            <version>${selenium.version}</version>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-chrome-driver</artifactId>
            <version>${selenium.version}</version>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-remote-driver</artifactId>
            <version>${selenium.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-transport-native-epoll</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-transport-native-kqueue</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-support</artifactId>
            <version>${selenium.version}</version>
        </dependency>

        <!-- Test scope -->
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-objectstore-connector</artifactId>
            <version>${muleObjectStoreConnectorVersion}</version>
            <classifier>mule-plugin</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest</artifactId>
            <version>2.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- MUnit plugin override: -Dotel.logs.exporter=none reaches the forked MUnit JVM.
                 Selenium ${selenium.version} pulls in opentelemetry-sdk-extension-autoconfigure,
                 which auto-discovers an OTLP logs exporter via system properties and fails because
                 the matching exporter jar isn't on the classpath. CLI -D doesn't propagate to the
                 forked JVM. skipAfterFailure=false so an early failure in one suite doesn't abort
                 the whole run. -->
            <plugin>
                <groupId>com.mulesoft.munit</groupId>
                <artifactId>munit-extensions-maven-plugin</artifactId>
                <configuration>
                    <skipAfterFailure>false</skipAfterFailure>
                    <argLines>
                        <argLine>-Dotel.logs.exporter=none</argLine>
                    </argLines>
                </configuration>
            </plugin>
            <!-- Mycila license:check inherited from grandparent (mule-modules-parent 1.5.0) is NOT
                 wired to ${skipVerifications}. Override here to disable license-header enforcement
                 for this round so we can revert formatter-only churn (header insertion) on files
                 we didn't semantically edit. Re-enable for the full-tree formatter pass before PR. -->
            <plugin>
                <groupId>com.mycila</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
