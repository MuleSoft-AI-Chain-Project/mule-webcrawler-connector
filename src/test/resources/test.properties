# NOTE: remote.browser.url below is a USER-TENANT-SPECIFIC CloudHub URL
# provisioned for the developer who started this workstream. If the tenant is
# decommissioned, or if a different developer clones this repo, the 6
# `remote-browser`-tagged MUnit tests will time out against a dead host
# (60s per test × 6 = ~6 min wasted CI time).
#
# To override without editing this file, pass at build time:
#   mvn clean install -Dremote.browser.url=https://your-remote-browser.example.com
# Or skip remote-browser tests entirely:
#   mvn clean install -Dmunit.tags=!remote-browser
#
# CI pipelines should set `-Dremote.browser.url=$REMOTE_BROWSER_URL` (or the
# `!remote-browser` tag exclusion) rather than relying on this default.
remote.browser.url=https://remote-browser-2-0iql6p.5sc6y6-1.usa-e2.cloudhub.io
test.url.static=https://example.com
test.url.iana=https://www.iana.org/
test.url.robots.denied=https://httpbin.org/deny
test.url.basic.auth=https://httpbin.org/basic-auth/user/passwd
