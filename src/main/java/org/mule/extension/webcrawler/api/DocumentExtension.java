package org.mule.extension.webcrawler.api;

/**
 * Document file extensions recognised by the crawler when classifying links and downloads.
 */
public enum DocumentExtension {
    PDF, DOC, DOCX, XLS, XLSX, PPT, PPTX, ZIP, RAR
}
